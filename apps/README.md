# Apps

These repo-local applications support startup services. They are not GNU Stow packages.

- `entrypoint/` starts profile services.
- `gdrive/` runs Google Drive synchronization through Docker Compose.
